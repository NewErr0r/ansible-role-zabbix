---
# The root user password for MariaDB
mariadb_root_pass: "P@ssw0rd"

# DB name for Zabbix
db_name: "zabbix"

# DB user name for Zabbix 
db_user_name: "zabbix"

# DB user pass for Zabbix 
db_user_pass: "P@ssw0rd"

# for php.ini
timezone: "Europe/Moscow"
max_execution_time: "300"
post_max_size: "16M"
max_input_time: "300"

# Repositoryes for Zabbix check 'https://repo.zabbix.com/zabbix'
repo_zabbix: "https://repo.zabbix.com/zabbix/5.0/rhel/8/x86_64/zabbix-release-5.0-1.el8.noarch.rpm"

# Configure nginx for zabbix
path_nginx_conf: /etc/nginx/nginx.conf
path_for_nginx_conf: /usr/share/zabbix